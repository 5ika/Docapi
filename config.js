var config = {
    // App infos
    app: {
        name: "DocAPI",
        port: 1280
    },
    // Base de données
    db: {
        server: 'mongodb://localhost/docapi'
    },
};
module.exports = config;
